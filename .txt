
    // Next module button logic
    const nextModuleButton = tooltip.querySelector("#nextModule");
    if (nextModuleButton) {
      nextModuleButton.addEventListener("click", () => {
        // Logic to move to the next module
      });
    }

    // Try again button logic
    const tryAgainButton = tooltip.querySelector("#tryAgain");
    if (tryAgainButton) {
      tryAgainButton.addEventListener("click", () => {
        quizContainer.innerHTML = "";
        quizQuestions.forEach((question, questionIndex) => {
          const userAnswerIndex = userSelections[questionIndex];
          const questionElement = createQuestionElement(
            question,
            questionIndex,
            userAnswerIndex
          );
          quizContainer.appendChild(questionElement);
        });
        quizContainer.appendChild(submitButton);
        if (quizContainer.contains(tooltip)) {
          quizContainer.removeChild(tooltip);
        }
      });
    }